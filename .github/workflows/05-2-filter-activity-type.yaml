name: 05 - 2- Event filters and Activity Types

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Running whenerve a PR is closed AND base branch is main"

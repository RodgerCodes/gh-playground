name: 01-building-blocks

on: workflow_dispatch

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "hello world"

    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Successfull step
              run: |
                echo "i will successf"
                exit 0
            - name: say goodbye
              run: echo "Goodbye"
name: 05 - 1- Event filters and Activity Types

on:
  pull_request:
    types:
      - opened
      - synchronized
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Running whenerve a PR is opened or synchronized AND base branch is main"
